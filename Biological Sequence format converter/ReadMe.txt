FastQ to FastA converter
_ A simple program that take in an inputted FASTQ format file and convert the content into FASTA format. 

FASTA to FASTQ converter
_ A simple program that take in an inputted FASTQ format file and convert the content into FASTA format. 